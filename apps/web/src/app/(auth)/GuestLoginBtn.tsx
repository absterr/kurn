"use client";
import { Button } from "@/components/ui/button";

const GuestLoginBtn = () => {
  return (
    <Button variant="outline" className="w-fit text-xs cursor-pointer">
      Continue as Guest
    </Button>
  );
};

export default GuestLoginBtn;
